# URL of the SQLite database service.
# In a multi-container setup, this points to the database container host and port.
# Example for local dev:
# DATABASE_SERVICE_URL=http://localhost:5001
# Example for docker-compose or platform networking:
# DATABASE_SERVICE_URL=http://todo_sqlite_database:5001
DATABASE_SERVICE_URL=
